# Devs on Wallstreet

A lightweight React + Vite landing page starter for a finance and technology brand.

## Run locally

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
